<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.sys.dao.SysLogDao">

	<select id="queryObject" resultType="io.renren.modules.sys.entity.SysLogEntity">
		select * from sys_log where id = #{value}
	</select>

	<select id="queryList" resultType="io.renren.modules.sys.entity.SysLogEntity">
		select * from sys_log 
		<where>
			<if test="key != null and key.trim() != ''">
				username like '%' || #{key} || '%'
				or 
				operation like '%' || #{key} || '%'
			</if>
			<if test="offset != null and limit != null">
			rownum &lt;= ${offset} + ${limit}
			</if>
		</where>
<if test="offset != null">
minus		
		select * from sys_log 
		<where>
			<if test="key != null and key.trim() != ''">
				username like '%' || #{key} || '%'
				or 
				operation like '%' || #{key} || '%'
			</if>
			rownum &lt;= ${offset}
		</where>
</if>
		order by id desc 
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from sys_log 
		<where>
			<if test="key != null and key.trim() != ''">
				username like '%' || #{key} || '%'
				or 
				operation like '%' || #{key} || '%'
			</if>
		</where>
	</select>
	 
	<insert id="save" parameterType="io.renren.modules.sys.entity.SysLogEntity" useGeneratedKeys="true" keyProperty="id">
		insert into sys_log
		(
			id,
			username, 
			operation, 
			method, 
			params,
			time,
			ip, 
			create_date
		)
		values
		(
			#{id}, 
			#{username}, 
			#{operation}, 
			#{method}, 
			#{params}, 
			#{time},
			#{ip},
			#{createDate}
		)
	</insert>
	
	<delete id="delete">
		delete from sys_log where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from sys_log where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>
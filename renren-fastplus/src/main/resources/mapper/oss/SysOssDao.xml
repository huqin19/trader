<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.oss.dao.SysOssDao">

	<select id="queryObject" resultType="io.renren.modules.oss.entity.SysOssEntity">
		select * from sys_oss where id = #{value}
	</select>

	<select id="queryList" resultType="io.renren.modules.oss.entity.SysOssEntity">
		select 
			* 
		from 
		( 
			select 
				rownum rowx, x.*  
			from			
				(select * from sys_oss o 
					<choose>
						<when test="sidx != null and sidx.trim() != ''">
							order by <!--o.-->${sidx} ${order}
						</when>
						<otherwise>
							order by o.id desc
						</otherwise>
					</choose>
				)x 
			<where>
				<if test="offset != null and limit != null ">
					rownum &lt;= ${offset} + ${limit}
				</if>
			</where>
		)
		<if test="offset != null ">
			where 
				rowx &gt; ${offset}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from sys_oss
	</select>
	 
	<insert id="save" parameterType="io.renren.modules.oss.entity.SysOssEntity">
		insert into sys_oss
		(
			id,
			url, 
			create_date
		)
		values
		(
			#{id,jdbcType=NUMERIC}, 
			#{url,jdbcType=VARCHAR}, 
			#{createDate,jdbcType=DATE}
		)
	</insert>
	 
	<update id="update" parameterType="io.renren.modules.oss.entity.SysOssEntity">
		update sys_oss 
		<set>
			<if test="url != null">url = #{url}, </if>
			<if test="createDate != null">create_date = #{createDate}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from sys_oss where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from sys_oss where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.wd.dao.CBondIssuerRatingDao">
	<select id="queryObject" resultType="io.renren.modules.wd.entity.CBondIssuerRatingEntity" >
		select * from wind_filesync.CBONDISSUERRATING where object_id = #{value}
	</select> 

	<select id="queryList" resultType="io.renren.modules.wd.entity.CBondIssuerRatingEntity" >
		select * from wind_filesync.CBONDISSUERRATING 
		<where>
			ann_dt like '%${annDt}%'
		</where>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(1) from wind_filesync.CBONDISSUERRATING 
		<where>
			ann_dt like '%${annDt}%' 
		</where>
	</select>
		<insert id="save" parameterType="io.renren.modules.wd.entity.CBondIssuerRatingEntity">
		insert into wind_filesync.CBONDISSUERRATING
		(
			`object_id`, 
			`s_info_compname`, 
			`ann_dt`, 
			`b_rate_style`,
			`b_info_creditrating`,
			`b_rate_ratingoutlook`, 
			`b_info_creditratingagency`,
			`s_info_compcode`,
			`b_info_creditratingexplain`,
			`b_info_precreditrating`,
			`b_creditrating_change`,
			`b_info_issuerratetype`,
			`ann_dt2`,
			`opdate`,
			`opmode`,
			`imp_date`,
			`imp_time`
		)
		values
		(
			#{objectId}, 
			#{sInfoCompname}, 
			#{annDt}, 
			#{bRateStyle}, 
			#{bInfoCreditrating},
			#{bRateRatingoutlook},
			#{bInfoCreditratingagency},
			#{sInfoCompcode},
			#{bInfoCreditratingexplain},
			#{bInfoPrecreditrating},
			#{bCreditratingChange},
			#{bInfoIssuerratetype},
			#{annDt2},
			#{opdate},
			#{opmode},
			#{impDate},
			#{impTime}		
		)
	</insert>
</mapper>